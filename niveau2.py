from niveau1.py import Minesweeper

# Generate random grids

# Hide non-revealed tiles

# Reveal "touching" '0' tiles when one is hit

# Change '0' tiles to blank spaces

# Make it playing from command-line by with inputs '(x, y)' # Use list(str.strip('(),'))
